// Tailwind 4 s'installe comme un simple plugin PostCSS.
// Aucune configuration JavaScript : le thème vit dans src/styles/tokens.css.
const config = {
  plugins: ["@tailwindcss/postcss"],
};

export default config;
